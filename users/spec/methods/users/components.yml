Name:
  $ref: 'shared://string'
  description: Имя/Отчество/Фамилия

Phone:
  $ref: 'shared://string'
  description: Год выхода

CreatedAt:
  $ref: 'shared://string'
  description: Дата создания

User:
  type: object
  description: Пользователь
  additionalProperties: false
  properties:
    id:
      $ref: 'shared://positiveinteger'
      description: ID
    firstName:
      $ref: '#/Name'
    middleName:
      $ref: '#/Name'
      nullable: true
    lastName:
      $ref: '#/Name'
    phone:
      $ref: '#/Phone'
    created_at:
      $ref: '#/CreatedAt'
