get:
  tags:
    - games
  summary: Получить список игр
  description: Получить список игр
  x-router-controller: games
  operationId: getGames
  parameters:
    - in: query
      name: limit
      required: true
      description: Limit
      schema:
        $ref: 'shared://limit'
  responses:
    200:
      description: Список игр
      content:
        application/json:
          schema:
            description: Строка ответа
            $ref: './components.yml#/Games'
